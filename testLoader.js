const { LocalStorage } = require('node-localstorage');


const testLoader= async function(source) {
    // const localStorage = new LocalStorage('./scratch');
    // localStorage.clear()
    return source
  };


module.exports = testLoader